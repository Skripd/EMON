type Measurement {
  id: ID! @id
  gasUsage: Int!
  usageHigh: Int!
  usageLow: Int!
  returnHigh: Int!
  returnLow: Int!
  currentUsage: Int!
  dateRecorded: DateTime! @unique
  dateAddedToDatabase: DateTime! @createdAt
}
